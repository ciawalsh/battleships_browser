<div>
		<% if @name && !@name.empty? %>
			Thank you <%= @name %>, you are now registered!
		<% else %>
			<form action ="/hello">
				What's your name?
				<input type="text" name="name">
				<input type="submit" value="submit">
			</form>
		<% end %>
</div>